USE sakila;
SELECT 
    MAX(amount) AS Maior,
    MIN (amount) AS Menor,
    AVG(amount) AS 'Media de Valores',
    SUM (amount) AS 'Total de Vendas',
    COUNT(amount) AS 'Numeros de Vendas'
FROM payment 
WHERE staff_id = 1;
